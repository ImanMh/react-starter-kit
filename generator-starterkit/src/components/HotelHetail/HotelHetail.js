import React from 'react';
import withStyles from 'isomorphic-style-loader/lib/withStyles';
import s from './HotelHetail.scss';

class HotelHetail extends React.Component {
  static propTypes = {};

  constructor(props, context) {
    super(props, context);
      this.state = {};
  }

  render() {
    const {} = this.props;
    return (
      <div className=''} >
      </div>
  );
  }
}

export default withStyles(s)(HotelHetail);
